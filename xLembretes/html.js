
// HTML - Anotações importantes.

// Pode ser letra minuscula ou maiuscula em HTML, ele é case insensitive. MAS a boa prática é manter um padrão -->


// Criar package.json
// Fazer esse comando dentro da pasta que quer: npm init -y

// No TERMINAL:

// Criar arquivo package.json: PS C:\apps\aulasWeb\ajax> npm init -y

// Para ver no browser, abra a porta no terminal: http-server .
// Caso queira mudar a porta no Terminal: http-server -p 9090 . 
// Desabilitar o cache no terminal: http-server -c-1 . (assim posso atualizar os módulos do meu HMTL e já atualizarão as páginas no browser)

// Navegar pelas pastas: cd nomeDaPasta
// Voltar uma pasta no terminal: cd ..
// Parar execução do terminal: CTRL + C + ENTER

// Ficar ouvindo na porta: dentro da pasta 'form' digita no terminal: node server.js
// Toda vez que alterar, salvar e chamar ele de novo para ouvir na porta. Ou então, usar o 'nodemom'.
// Para instalar a dependência 'nodemon' dentro da pasta 'form' digita no terminal: npm i --save nodemon
// Agora para ele ficar ouvindo, e atualizando a página automaticamente: nodemon server.js

// Instalar as dependencias do Express e do Parser dentro de 'form'
// PS C:\apps\aulasWeb\html\exercicios\form> npm i --save express@4.16.3 body-parser@1.18.2 -E

// Instalar dependencia:
// PS C:\apps\aulasWeb\react\crud\backend> npm i --save json-server@0.13.0 -E



